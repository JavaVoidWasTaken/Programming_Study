#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>
#include <string>
using namespace std;
using ll = long long;

const int maxArraySize = 1000000000;

int getHayScore(:

int main() {

	int testSize, temp;
	cin >> testSize;

	int hay[maxArraySize];
	int maxHay[maxArraySize];

	for (int i = 0; i < testSize; ++i){
		cin >> hay[i];
	}

	for (int i = 0; i < testSize; ++i){
		cout << hay[i];
	}

	return 0;
}

